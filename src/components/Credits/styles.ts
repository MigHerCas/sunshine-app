import styled from 'styled-components';

export const Credits = styled.div`
  position: fixed;
  bottom: 2rem;
  left: 2rem;
  z-index: -1;

  @media (min-width: 1024px) {
    top: 3rem;
    left: 3rem;
  }
`;
